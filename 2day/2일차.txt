아마존 DB를 생성하고 putty와 사용하는 방법
https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/putty.html?icmpid=docs_ec2_console

아마존에서 .pem 파일을 만든다.
puttygen으로 key를 만들때 load를 먼저 한다.
load 하는 파일은 .pem파일로 한다.
그 후에 save private key를 가지고 .ppk 파일을 만든다.